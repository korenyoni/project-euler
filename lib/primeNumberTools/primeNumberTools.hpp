#ifndef PRIME_NUMBER_TOOLS_H
#define PRIME_NUMBER_TOOLS_H
#include <vector>
#include "primeNumberTools.cpp"

std::map<int,int> getPrimeFactorsExponentMap(int number);
// get the prime factors, as integers of the specified (int) number

#endif
